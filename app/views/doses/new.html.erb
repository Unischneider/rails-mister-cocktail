
<div class="dose-box">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <div class="form-group">
      <%= f.input :description %>
      <%= f.association :ingredient %>
    </div>
    <div class="form-group dose-button">
      <%= f.button :submit %>
    </div>
  <% end %>
</div>
